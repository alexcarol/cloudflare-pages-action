{
  "name": "cloudflare-pages-action",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "cloudflare": "^5.2.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
